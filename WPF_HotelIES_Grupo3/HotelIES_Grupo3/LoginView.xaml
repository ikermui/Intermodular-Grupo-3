<Window x:Class="HotelIES_Grupo3.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelIES_Grupo3"
        mc:Ignorable="d"
        Title="LoginView" Height="720" Width="1280" WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Image Grid.Column="1" Grid.RowSpan="3" Stretch="Fill" Source="/habitacionHotel2.jpg"/>

        <!--Iniciar sesion-->

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Column="0" Grid.Row="0">
            <Image Source="/logo.png" Margin="0,20,0,0"></Image>
        </StackPanel>

        <StackPanel Grid.Column="0" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.RowSpan="2" Margin="0,35,0,0" Width="300" Height="400">
            <WrapPanel>
                <Button HorizontalAlignment="Left" Background="Transparent" BorderBrush="#1f3b91" Margin="0,0,10,0" BorderThickness="0,0,0,2" FontSize="20">
                    Iniciar sesión
                </Button>
            </WrapPanel>

            <Label FontSize="18">Email</Label>
            <Border BorderBrush="Gray" CornerRadius="4" BorderThickness="1" Background="#F0F0F0">
                <TextBox x:Name="txtEmail" BorderThickness="0" Background="Transparent" FontSize="18"></TextBox>
            </Border>

            <Label FontSize="18">Contraseña</Label>
            <Border BorderBrush="Gray" CornerRadius="4" BorderThickness="1" Background="#F0F0F0">
                <PasswordBox x:Name="txtPassword" BorderThickness="0" Background="Transparent" FontSize="18"></PasswordBox>
            </Border>

            <Button Margin="0,20,40,0" Background="Transparent" BorderBrush="Transparent" Foreground="#1f3b91" FontSize="18" HorizontalAlignment="Left">Recordar contraseña</Button>

            <Border CornerRadius="20" Background="#1f3b91" BorderBrush="#1f3b91" BorderThickness="1" Margin="0,40,0,0" Width="140" Height="45">
                <Button HorizontalAlignment="Center" VerticalAlignment="Bottom" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Foreground="white" FontWeight="bold" FontSize="20" Click="BtnIniciarSesion_Click" Margin="0,0,0,7">
                    Iniciar sesión
                </Button>
            </Border>
        </StackPanel>

        <!--Registrarse-->

    </Grid>
</Window>
